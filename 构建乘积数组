/*
给定一个数组A[0,1,...,n-1],请构建一个数组B[0,1,...,n-1],其中B中的元素B[i]=A[0]*A[1]*...*A[i-1]*A[i+1]*...*A[n-1]。不能使用除法
*/
/*
   
b0  1. a1.a2  an
b1. a0 1 a2. a3
b2. a0 a1 1. a3
bn. a0 a1 a2  1
列出上面点表格，可以看到以1为分界线，分割两个三角，然后上下两个三角分别求积。
*/
class Solution {
public:
    vector<int> multiply(const vector<int>& A) {
    
    }
};
